import { Container } from "../shared/Container";
import { Title } from "../shared/Title";

export const Brands = () => {
    return <section>
        <Container className="space-y-8">
            <div>
                <Title>Testing</Title>
            </div>
        </Container>
    </section>
}